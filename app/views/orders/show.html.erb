<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @order.number %>
</p>
<p>
  <strong>Client:</strong>
  <%= @order.client %>
</p>
<p>
  <strong>Add_info:</strong>
  <%= @order.add_info %>
</p>
<h2>the lines of this order </h2>
<table>
  <thead>
    <tr>
      <th>Order</th>
      <th>Good</th>
      <th>Countity</th>
      <th>Price</th>
      <th>Sum</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <% @order_lines.each do |order_line| %>
      <tr>
        <td><%= order_line.order_id %></td>
        <td><%= order_line.good %></td>
        <td><%= order_line.countity %></td>
        <td><%= order_line.price %></td>
        <td><%= order_line.sum %></td>
        <td><%= link_to 'Edit', edit_order_line_path(order_line) %></td>
        <td><%= link_to 'Destroy', order_line, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
     </tbody>
</table>
 
<%= link_to 'New Order Line', new_order_line_path(:order_id => @order.id) %>
<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
